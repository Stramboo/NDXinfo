# ===========================================
# NASDAQ 每日分析报告 - 最小依赖（仅供 GitHub Actions 使用）
#
# 此文件仅包含 nasdaq_analyzer.py 生成每日报告
# 所必需的核心依赖，不含 GUI（PyQt5）、实盘交易
#（alpaca-py）等仅桌面 App 需要的重型包。
#
# 完整项目依赖见 requirements.txt
# ===========================================

# --- 核心必需 ---
yfinance>=0.2.0          # 美股行情（纳斯达克、个股、VIX）
pandas>=2.0.0            # 数据处理与技术指标计算
numpy>=1.24.0            # 数值计算（pandas 依赖，显式声明防止版本冲突）
jinja2>=3.1.0            # HTML 报告模板引擎

# --- 可选：新闻情绪分析的新闻数据源 ---
# 若 GitHub Actions 上安装 akshare 失败（依赖链重），
# nasdaq_analyzer.py 会自动跳过新闻情绪模块，不影响报告主体。
# 如需新闻情绪，可取消下方注释：
# akshare>=1.10.0
