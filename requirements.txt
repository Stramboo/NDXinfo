# ===========================================
# NASDAQ 项目 - 完整依赖（含桌面 GUI + 实盘交易）
#
# ├─ 核心报告 → requirements-report.txt（最简，GitHub Actions 用）
# ├─ 桌面交易 → requirements.txt（本文件，本地开发用）
# └─ 详细说明 → README.md
# ===========================================
#
# --- 核心依赖（必需）---
yfinance>=0.2.0          # 美股行情数据源
pandas>=2.0.0            # 数据处理与技术指标计算
jinja2>=3.1.0            # HTML 报告模板引擎

# --- 可选依赖：新闻情绪分析 ---
# pip install akshare
akshare>=1.10.0          # A股/港股新闻数据源（ENABLE_NEWS_SENTIMENT）

# --- 可选依赖：港股/A股数据 ---
# 已由 akshare 覆盖（ENABLE_HK_A_SHARE）

# --- 可选依赖：ML 预测（默认关闭 ENABLE_ML_PREDICT）---
# pip install scikit-learn
# scikit-learn>=1.3.0     # 机器学习价格预测

# ===========================================
# 自动交易模块依赖
# ===========================================

# --- 券商 API ---
alpaca-py>=0.20.0         # Alpaca 券商 SDK（实盘/模拟交易）

# --- GUI 桌面界面 ---
PyQt5>=5.15.0             # 跨平台桌面 GUI 框架
pyqtgraph>=0.13.0         # 高性能金融图表（K线、指标叠加）
